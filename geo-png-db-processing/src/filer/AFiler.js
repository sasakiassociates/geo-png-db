class AFiler {
    ensureDir(dir, callback) {
        console.error('ensureDir not implemented');
    }

    fileExists(file, callback) {
        console.error('fileExists not implemented');
    }

    writeFile(path, data, o, c) {
        console.error('writeFile not implemented');
    }

    readFile(path, o, c) {
        console.error('readFile not implemented');
    }

    copyFile(fromPath, toPath, o, c) {
        console.error('copyFile not implemented');
    }

    readdir(path, o, c) {
        console.error('readdir not implemented');
    }

    saveImage(path, image, callback) {
        console.error('saveImage not implemented');
    }

    readImage(path, callback) {
        console.error('readImage not implemented');
    }
}

module.exports = AFiler;

